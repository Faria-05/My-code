#include<stdio.h>
int main()
{
    int marks, agg=0, ave, i;
    for(i=1; i<=5; i++)
    {
        printf("Enter the marks:");
        scanf("%d",&marks);
        if(marks<=100 && marks>=0)
        {
agg+=marks;
}
else
{
    printf("Enter marks 00-100 and try again\n");
}

}   
ave=agg/5;
printf("Aggregate mark is=%d  \n Average mark is=%d",agg,ave);
return 0;
}
