#include<stdio.h>
int main()
{
    int a[100],i=0,n,j;
    printf("Enter the number");
    scanf("%d",&n);
    while(n>0)
    {
        a[i]=n%10;
        n=n/10;
        i++;
    }
    printf("The reverse number is:");
    for(j=0;j<i;j++)
    {
        printf("%d",a[j]);
    }
    return 0;
}
