#include<stdio.h>
int main()
{
    int a[5],i,sum=0;
    float ave;
    printf("Enter marks:");
    for(i=0;i<5;i++)
    {
        scanf("%d",&a[i]);
       
    }
    printf(".........\n");
    for(i=0;i<5;i++)
    {
        
        printf("%d\n",a[i]);
         sum+=a[i];
    }
    ave=sum/5;
     
    printf("sum=%d\n",sum);
    printf("ave=%.2f",ave);
return 0;
}
